global:
  scrape_interval: 2s
  evaluation_interval: 2s

scrape_configs:
  - job_name: 'vetra'
    static_configs:
      - targets: ['vetra-api:8080']
        labels:
          app: 'vetra-control-plane'

  - job_name: 'vllm'
    static_configs:
      - targets: ['host.docker.internal:8000']
        labels:
          app: 'vllm-engine'
